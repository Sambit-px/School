<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Nearest Schools</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            padding: 2rem;
            background-color: #f5f5f5;
        }

        h2 {
            color: #333;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 1rem;
        }

        th,
        td {
            border: 1px solid #ccc;
            padding: 0.75rem;
            text-align: left;
        }

        th {
            background-color: #f0f0f0;
        }

        tr:nth-child(even) {
            background-color: #fafafa;
        }

        a {
            display: inline-block;
            margin-top: 1rem;
            text-decoration: none;
            color: #0066cc;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>

    <h2>Nearest Schools to "<%= location %>"</h2>

    <table>
        <tr>
            <th>Name</th>
            <th>Address</th>
            <th>Distance (km)</th>
        </tr>
        <% schools.forEach(s=> { %>
            <tr>
                <td>
                    <%= s.name %>
                </td>
                <td>
                    <%= s.address %>
                </td>
                <td>
                    <%= s.distance.toFixed(2) %>
                </td>
            </tr>
            <% }) %>
    </table>

    <a href="/">← Back to All Schools</a>

</body>

</html>